- @album.images.each do |image|
  %p
    = link_to(image_tag(image.photo.url(:thumb)), image.photo.url , :class => "fancybox")

:javascript
  $(document).ready(function() {
    jQuery(function() {
    $("a.fancybox").fancybox();
    });
  });
  
